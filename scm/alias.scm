(define ceiling Ceil)
(define constant (lambda (n) n))
(define klankSpec klankDataMce)
(define mce asMce)
(define mrg mrg-n)
(define negate Neg)
(define uclone clone)

;(define list-ref !!)
(define head car)
(define tail cdr)


(define div /)
